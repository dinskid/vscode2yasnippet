# -*- mode: snippet -*-
# name: binexp
# key: libBINPOW
# --
const int md = 1e9 + 7;
long long binpow(long long a, long long b, long long mod = md) {
	a %= mod;
	long long res = 1;
	while (b > 0) {
		if (b & 1)
			res = res * a % mod;
		a = a * a % mod;
		b >>= 1;
	}
	return res;
}